import ReactDOM from "react-dom";

import "./index.css";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));

//second arguement is default javascript Dom object content of div is replaced by <App/>
//so whatever inside DIV is replaced by <App /> which is a component
//props -> properties
